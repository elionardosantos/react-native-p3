import { StyleSheet } from 'react-native';

export const styledInfoText = StyleSheet.create({
  container: {
    marginHorizontal: 20,
    marginTop: 5,
    marginBottom: 10,
  },
  text: {
    color: '#888',
    fontSize: 18,
    fontWeight: 600,
  },
});