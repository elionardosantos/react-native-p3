import { StyleSheet } from 'react-native';

export const styledTitle = StyleSheet.create({
  container: {
    marginHorizontal: 20,
    marginVertical: 10,
  },
  title: {
    color: '#FFFFFF',
    fontSize: 35,
    fontWeight: 800,
  },
});