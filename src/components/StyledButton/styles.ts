import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginHorizontal: 20,
        marginTop: 15,
    },
    button: {
        backgroundColor: "#d50606",
        borderRadius: 6,
    },
    text: {
        color: "#fff",
        textAlign: "center",
        margin: 15,
        fontWeight: 500,
        fontSize: 15,
    },
});