import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginHorizontal: 20,
        marginTop: 15,
    },
    input: {
        color: '#ccc',
        borderColor: '#888',
        backgroundColor: '#170909',
        fontSize: 15,
        fontWeight: 500,
        borderWidth: 1,
        borderRadius: 6,
        height: 60,
        paddingLeft: 15,
    }
});
